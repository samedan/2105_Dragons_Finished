const SECONDS = 1000;
const MINUTES = SECONDS * 60;
const HOURS = MINUTES * 60;
const DAYS = HOURS * 60;

const REFRESH_RATE = 5; // this is 5 units of smth
const STARTING_BALANCE = 50;

module.exports = {
  SECONDS,
  MINUTES,
  HOURS,
  DAYS,
  REFRESH_RATE,
  STARTING_BALANCE,
};
